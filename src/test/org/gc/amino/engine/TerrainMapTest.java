package org.gc.amino.engine;

import org.gc.amino.util.Util;
import org.junit.Before;
import org.junit.Test;

public class TerrainMapTest {
    
    @Before
    public void init() {
         Util.initForUnitTesting();
    }
    
    @Test
    public void TerrainMap() {
//        assertEquals( "v0 v1", Format.joinValues( Func.newMap( "orange", Func.newList( "v0", "v1" ) ), " " ) );
    }
    
}
